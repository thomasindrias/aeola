services:
  agent-store:
    build: .
    ports:
      - "8000:8000"
    environment:
      - API_KEY=${API_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - DB_PATH=/app/data/agent-store.db
    volumes:
      - agent-store-data:/app/data
    ipc: host

volumes:
  agent-store-data:
