services:
  aeola:
    build: .
    ports:
      - "8000:8000"
    environment:
      - API_KEY=${API_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - DB_PATH=/app/data/aeola.db
    volumes:
      - aeola-data:/app/data
    ipc: host

volumes:
  aeola-data:
